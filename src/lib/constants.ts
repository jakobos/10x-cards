/**
 * Maximum length for flashcard front text (question/term)
 */
export const MAX_FLASHCARD_FRONT_LENGTH = 200;

/**
 * Maximum length for flashcard back text (answer/definition)
 */
export const MAX_FLASHCARD_BACK_LENGTH = 500;

/**
 * Minimum length for AI generation source text
 */
export const MIN_SOURCE_TEXT_LENGTH = 1000;

/**
 * Maximum length for AI generation source text
 */
export const MAX_SOURCE_TEXT_LENGTH = 10000;
